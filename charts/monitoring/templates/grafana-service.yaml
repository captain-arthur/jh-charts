{{- if .Values.grafana.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-grafana
  labels:
    {{- include "monitoring.labels" (dict "root" . "component" "grafana") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      name: http
  selector:
    {{- include "monitoring.labels" (dict "root" . "component" "grafana") | nindent 4 }}
{{- end }}
